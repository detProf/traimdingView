FROM python:3.10-slim

WORKDIR /app/strategies

# Copy only strategy engine files
COPY strategies /app/strategies

RUN pip install --no-cache-dir --progress-bar off -r requirements.txt

EXPOSE 5003

CMD ["python", "ai_strategy.py"]
